import 'package:flutter/material.dart';
import 'package:hello_world/widgets/scaffold/drawer/page.dart';

class SettingsPageInterface extends StatelessWidget {
  const SettingsPageInterface({super.key});

  @override
  Widget build(BuildContext context) {
    return SettingsPage(
      title: '界面',
      children: [
        
      ]
    );
  }
}