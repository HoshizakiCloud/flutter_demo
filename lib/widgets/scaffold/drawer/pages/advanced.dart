import 'package:flutter/material.dart';
import 'package:hello_world/widgets/scaffold/drawer/page.dart';

class SettingsPageAdvanced extends StatelessWidget {
  const SettingsPageAdvanced({super.key});

  @override
  Widget build(BuildContext context) {
    return SettingsPage(
      title: '高级',
      children: [
        
      ]
    );
  }
}